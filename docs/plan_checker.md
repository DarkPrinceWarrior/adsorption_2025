Отличный план! Вижу, что агент глубоко вник в теорию. Но есть **одна критическая ошибка в понимании архитектуры**, которая пронизывает пункты 6-7. Разберу по порядку:

---

## ✅ ЧТО ОЧЕНЬ ХОРОШО (пункты 1-5, 8-9)

### 1. Валидация данных перед обучением
**Абсолютно правильно!** Это must-have. Но с уточнением:
- Не `ValueError` сразу, а **два режима**: strict (падаем) и warning (логируем + продолжаем)
- Пользователь должен видеть, ЧТО не так: "Образец #42: a₀ отклоняется на 5% от 28.86×W₀"

### 2. Новые engineered фичи (пункт 3)
**Отлично!** C_metal, C_ligand, log_C, R_molar, T_range - всё нужно.
- ⚠️ Важно: для R_molar нужен **fallback** на безводную соль, если гидрат неизвестен (с warning)

### 3. Молярные массы и стехиометрия (пункт 4)
**КРИТИЧНО ВАЖНО!** Но есть нюанс:
```python
# Сейчас в коде:
n_ratio.clip(0.45, 2.3)  # Единый клип для ВСЕХ MOF!

# Правильно (учитывать химию):
if (metal, ligand) == ('Cu', 'BTC'):
    target_ratio = 1.5  # HKUST-1: Cu₃(BTC)₂
    bounds = (1.35, 1.65)  # ±10%
elif (metal, ligand) == ('Zn', 'BDC'):
    target_ratio = 2.0  # MOF-5
    bounds = (1.8, 2.2)
```

Это означает, что `_project_stoichiometry` должна **подгонять к конкретному теоретическому отношению**, а не к общему диапазону!

### 4. Температурные ограничения (пункт 5)
**Правильно, НО**:
- Модель предсказывает **КАТЕГОРИИ** (Tsyn_Category), а не числа!
- Для проверки `T_syn < T_boiling` нужны числа °C
- **Варианты**:
  1. Добавить стадии предсказания ЧИСЛЕННЫХ температур (не только категорий)
  2. ИЛИ конвертировать категории в средние значения бинов при post-processing

### 5. Тесты (пункт 9)
**Отлично!** Особенно test_stoichiometry.py (проверка молярных масс).

---

## ❌ КРИТИЧЕСКАЯ ОШИБКА (пункты 6-7)

### Проблема: Путаница между ВХОДАМИ и ВЫХОДАМИ модели

**Что модель ПОЛУЧАЕТ на вход:**
```python
# Адсорбционные дескрипторы (СЭХ) - ВХОДЫ:
W0, E0, x0, a0, E, S_BET, Ws, Wme, Sme
+ производные: Adsorption_Energy_Ratio, S_BET_E, и т.д.
```

**Что модель ПРЕДСКАЗЫВАЕТ:**
```python
# ВЫХОДЫ:
- Металл, Лиганд (categorical)
- m(соли), m(кислоты), V(растворителя) (continuous)
- Tsyn_Category, Tdry_Category, Treg_Category (categorical)
- + производные: K_equilibrium, Delta_G (через термодинамику)
```

### Агент предлагает в пункте 7:
> "После project_thermodynamics добавить блок, проверяющий **Ws≥W0, a₀=28.86·W₀, E=E₀/3**: если предсказания нарушают равенства, корректировать зависимую переменную"

**ОШИБКА:** Ws, W0, a₀, E, E₀ - это **ВХОДЫ**, модель их НЕ предсказывает! Их проверять/корректировать при inference не имеет смысла.

### Правильный подход:

**ДО ОБУЧЕНИЯ (пункт 2 плана):**
```python
# Валидация ВХОДНЫХ СЭХ
validate_SEH_data(df):
    check: a₀ ≈ 28.86 × W₀  (±1%)
    check: E ≈ E₀ / 3       (±10%)
    check: Ws ≥ W₀          (всегда)
    → если нарушено: warning/error
```

**ПРИ ОБУЧЕНИИ (пункт 6):**
```python
# Взвешивание сэмплов по физической консистентности ВХОДОВ
penalties = check_input_consistency(X_train)  # a₀, E, Ws
sample_weights = 1.0 + physics_weight * penalties
→ физически корректные сэмплы получают больший вес
```

**ПРИ INFERENCE (пункт 7):**
```python
# Корректировать ТОЛЬКО ВЫХОДЫ модели:
1. project_thermodynamics()  # K_eq, ΔG ✅
2. _project_stoichiometry()  # n_ratio, m(кислоты) ✅
3. _enforce_temperature_order()  # T_syn ≤ T_dry ≤ T_reg ✅
4. check_temperature_boiling()  # T_syn < T_boiling(solvent) ✅

# НЕ трогать входные СЭХ! ❌
```

---

## 🔧 ЧТО ИСПРАВИТЬ В ПЛАНЕ

### Пункт 6 (Physics-aware обучение):
**Было:**
> Рассмотреть дополнительный регуляризатор: возвращающую штраф по каждому сэмплу (аналог physics_loss_SEH)

**Должно быть:**
- ✅ Использовать physics_loss для **взвешивания сэмплов** по консистентности ВХОДНЫХ СЭХ
- ✅ Сэмплы с `a₀ ≈ 28.86×W₀` и `E ≈ E₀/3` → больший вес
- ❌ НЕ пытаться добавлять physics_loss в функцию потерь для предсказания СЭХ (мы их не предсказываем!)

### Пункт 7 (Inference post-processing):
**Было:**
> После project_thermodynamics добавить блок, проверяющий Ws≥W0, a₀=28.86·W₀, E=E₀/3

**Должно быть:**
```python
# Корректировать ТОЛЬКО предсказанные параметры:
1. project_thermodynamics()  # K_eq, ΔG
2. _project_stoichiometry()  # n_ratio (специфично для MOF!)
3. _enforce_temperature_order()  # категории
4. NEW: check_temperature_vs_boiling()  # T_syn < T_boiling
5. NEW: _enforce_stoichiometry_targets()  # подгонка к теор. отношению

# НЕ корректировать входные СЭХ (W0, a0, E, E0)!
```

---

## 📋 МОЙ ПЕРЕРАБОТАННЫЙ ПЛАН (PRIORITY)

### 🔴 PRIORITY 1 (Must have):
1. **data_validation.py**: валидация СЭХ ДО обучения (a₀, E, Ws)
2. **stoichiometry.py**: молярные массы нитратов + теоретические отношения
3. **Расширить add_salt_mass_features**: C_metal, C_ligand, R_molar, T_range
4. **test_stoichiometry.py**: тесты на молярные массы
5. **test_validation.py**: тесты на валидацию СЭХ

### 🟡 PRIORITY 2 (Should have):
6. **_project_stoichiometry**: подгонка к специфичному теор. отношению (не 0.45-2.3!)
7. **Температурные проверки**: T_syn < T_boiling (при валидации + inference)
8. **Sample weighting**: взвешивание по консистентности входных СЭХ
9. **Логирование**: physics_stats в StageResult.metrics

### 🟢 PRIORITY 3 (Nice to have):
10. **EqualityConstraint**: новый тип constraint для точных равенств
11. **README**: добавить теорию Дубинина, ссылки на docs
12. **test_physics_projection.py**: проверка коррекций при inference

---

## 💡 КОНКРЕТНЫЕ РЕКОМЕНДАЦИИ

### 1. Не путать входы и выходы!
```python
# ✅ ВХОДЫ (проверять ДО обучения):
a₀, W₀, E, E₀, x₀, S_BET, Ws, Wme, Sme

# ✅ ВЫХОДЫ (корректировать при inference):
Металл, Лиганд, m(соли), m(кислоты), V(р-ля), T_syn/dry/reg

# ✅ ПРОИЗВОДНЫЕ выходы (корректировать):
K_eq, ΔG, n_ratio, n_соли, n_кислоты
```

### 2. Специфичная стехиометрия для каждого MOF
```python
THEORETICAL_STOICHIOMETRY = {
    ('Cu', 'BTC'): (1.5, 0.15),   # target ± tolerance
    ('Zn', 'BDC'): (2.0, 0.20),
    ('Al', 'BTC'): (1.0, 0.10),
    # ...
}
```

### 3. Температуры: добавить численное предсказание?
Сейчас только категории → сложно проверить `T_syn < T_boiling`.  
**Вариант**: добавить регрессионные стадии для T_syn_numeric, T_dry_numeric, T_reg_numeric.

---

## 🎯 ИТОГ

**План агента на 80% отличный!** Проблема только в непонимании, что:
- **a₀, W₀, E, E₀** - это ВХОДЫ (адсорбционные данные из эксперимента)
- **Металл, массы, температуры** - это ВЫХОДЫ (то, что мы предсказываем)

**Мой вердикт**: 
- ✅ Взять пункты 1-5, 8-9 как есть
- ⚠️ Переработать пункты 6-7: убрать манипуляции с входными СЭХ, сфокусироваться на выходах
- ✅ Добавить специфичную стехиометрию для каждого типа MOF

Начинать реализацию с валидации данных + молярных масс + новых фич. Это даст максимальный эффект!